# قائمة تقدم مشروع مترجم بيان

## الحالة الحالية

المسار النشط الآن هو **قواعد د. خالد الكحسة الرسمية فقط**. أصبحت مفردات الإصدار القديم مثل `دع` و`نهاية` غير نشطة في Lexer الرسمي، وتبقى مصادر الإصدار الأول لأغراض الأرشفة فقط ولا تشغّل في مجموعة الاختبارات الافتراضية.

| المرحلة | الحالة | الدليل المتحقق |
| --- | --- | --- |
| توثيق قواعد الدكتور وتعليمات التكليف | مكتملة | ملاحظات استخراج الصفحات 1–13 محفوظة في `docs/doctor_grammar_extraction_notes.md`. |
| فصل Core وCLI | مكتملة | مشاريع Core وCLI مستقلة وقابلة للبناء. |
| المفردات وAST الرسمية | مكتملة مبدئياً | دعم الثوابت والأنواع والقوائم والسجلات والإجراءات والمعاملات والوصول والقيم الحرفية. |
| LL(1) وParse Tree الرسميان | مكتملة مبدئياً | `DoctorLl1Parser` جدولـي، ينتج Trace وParse Tree وAST، مع اختبارات للقواعد الرئيسية واسترداد فاصلة منقوطة مفقودة. |
| الدلالة وجدول الرموز | قيد التوسعة | `DoctorSemanticAnalyzer` يسجل الثوابت والأنواع والمتغيرات والإجراءات، ويتحقق من الثوابت ومعاملات الإجراءات ويكتب جدول رموز حقيقياً. |
| TAC وMIPS الرسميان | قيد التوسعة | يدعم المسار الرسمي فعلياً صحيح/حقيقي/منطقي/حرفي وخيط رمزي ثابت، والطباعة والإدخال للأنواع العددية والمحارف، والتحكم والحلقات، والأس الثابت غير السالب، والإجراءات غير العودية، والقوائم والسجلات المباشرة ذات العناصر/الحقول المفردة، بما فيها العناصر والحقول الحقيقية عبر FPU. |
| محرر Windows Forms الحديث المتصل بـCLI | قيد التوسعة | أضيف `CliCompilationClient`؛ يكتب المصدر مؤقتاً ويستدعي `Bayan.Compiler.Cli` ويعرض tokens وAST وLL(1) وParse Tree وSymbol Table وTAC وMIPS والتشخيصات من artifacts حقيقية. |
| عينات رسمية وتسليم نهائي | قيد التوسعة | أصبحت العينات الرسمية من `11` إلى `25` متاحة؛ تجاوز العدد الأدنى المطلوب (25 عينة إجمالاً) ما زال يحتاج جرداً نهائياً قبل التسليم. |

## تحقق آخر دفعة

- [x] تشغيل مجموعة قواعد الدكتور الافتراضية: **25 اختباراً ناجحاً**.
- [x] تحقق بعد إصلاح مسار Windows: نجح بناء Core وCLI وتشغيل الاختبارات الرسمية **25/25** في Linux؛ لا يزال تحقق WinForms النهائي يتطلب Windows.
- [x] تشغيل CLI على العينة الرسمية؛ كتب `tokens.txt` و`ast.txt` و`ll1-table.txt` و`ll1-trace.txt` و`parse-tree.txt` و`symbol-table.txt` و`manifest.json`.
- [x] التحقق من أن manifest الرسمي يعلن `CompletedStage = Semantic` و`DiagnosticCount = 0` للعينة السليمة.
- [x] التحقق من أن جدول الرموز يحتوي الثابت والأنواع والمتغيرات والإجراء الفعلية.
- [x] تحقق Release التراكمي: نجح بناء Core وCLI وتشغيل **25 اختباراً** رسمياً.
- [x] تكامل CLI النهائي: عينة `25_symbolic_string_input.bayan` نجحت حتى `MIPS` وكتبت `program.asm` وmanifest؛ عينة الخطأ النحوي عادت برمز خروج `1` وكتبت manifest للأخطاء.
- [x] تحويل subset رسمي من AST إلى TAC: تعريفات وقيم وطباعة وإدخال وتحكم وحلقات وإجراءات وقوائم وسجلات مباشرة.
- [x] توليد وتنفيذ MIPS رسمي في SPIM: تحقق ناتج المجموع `15` والحلقات `0` والإدخال `8` والأس `11` والإجراء المرجعي `5` والقائمة/السجل `7` ثم `X` والخيط الرمزي `بيان` وإدخال الخيط `Bayan` والحقيقي `3.50000000` وإدخال الحقيقي `3.00000000` والحقيقي المركب `2.00000000` ومقارنة الحقيقي `1` وإجراء الحقيقي `2.00000000`.
- [x] تشخيص وإصلاح اختبار إجراء الحقيقي: الاسم الأول «أضف» كلمة محجوزة للخطوة، فاستبدل بالمعرف «اجمع» ثم مرّ LL(1) وTAC وMIPS وSPIM.
- [x] تثبيت اتجاه معاملات المقارنة الرسمي في الاختبارات: الرمز `<` يترجم إلى «أكبر من» والرمز `>` إلى «أصغر من» وفق العرض العربي المعتمد في Lexer وقواعد الدكتور.
- [ ] إضافة الوصول المركب المتداخل، وتمرير القوائم/السجلات للإجراءات، والإجراءات العودية أو إطار مكدس كامل، والتحقق الحدودي في وقت التشغيل للقوائم.
- [ ] بناء Windows Forms في Visual Studio واختبار تكامل CLI.
- [ ] توسيع الاختبارات والعينات، وإنشاء تقرير PDF وحزمة تسليم بعد تثبيت الاسم GN.
- [x] إعادة تسمية ملفات العينات ومجلداتها إلى ASCII English آمن مع امتداد `.bayan`، وتحديث كل المراجع والاختبارات لتفادي تلف ترميز أسماء ZIP في Windows.
- [x] اعتماد اتجاه **Bayan Source Studio** كهوية حديثة للمحرر، مع مسار مراحل مرئي مستوحى من الخيار A.
- [x] إضافة شاشة ترحيبية قبل المحرر: هوية «بيان»، لمحة CLI → LL(1) → MIPS، وأزرار برنامج جديد وفتح `.bayan` وعينات المشروع.
- [x] إعادة تنظيم المحرر إلى Bayan Source Studio: مساحة مصدر هادئة، زر رئيسي للتحليل والترجمة، مسار مراحل جانبي، ولوحة تشخيصات وظيفية، من دون مخرجات ثابتة أو ارتباط مباشر بـCore.
- [x] اعتماد تخطيط **Compiler Journey**: شاشة محتوى واحدة لكل مرحلة من المصدر حتى MIPS، بدلاً من خلط أعمدة وتبويبات ومسار مراحل في وقت واحد.
- [x] تنفيذ تخطيط Compiler Journey في `CompilerJourneyForm`: محتوى واحد لكل مرحلة، وصفوف حصرية للهوية والأوامر والمحتوى ومسار الرحلة، من دون `SplitContainer` أو تبويبات ظاهرة في المسار النشط.
- [x] تعريف نظام تصميم موحد للواجهة الجديدة: لوحة ألوان، خطوط، مقياس مسافات، أحجام عناصر، تباين، وحالات تركيز/تحويم/تعطيل موثقة في `COMPILER_JOURNEY_DESIGN_SYSTEM.md`.
- [x] تنفيذ حالات واجهة واضحة: بداية فارغة، تشغيل CLI، نجاح الترجمة، خطأ التشخيص، وعدم العثور على CLI، من دون نتائج ثابتة مضللة.
- [ ] فحص بصري على Windows لأحجام نافذة صغيرة ومتوسطة وكبيرة، والتحقق من المحاذاة والقص والتداخل وقابلية قراءة النص العربي وMIPS.
- [x] تحديث دليل تجربة المحرر ورسائل العينات إلى العينة الرسمية `12_official_basic_execution.bayan` أو عينات 11–25، وعدم تقديم العينات المؤرشفة 01–10 على أنها برامج قواعد رسمية سليمة.
- [ ] تنفيذ Rebuild وتشغيل يدوي لشاشة الترحيب ومسار المراحل في Visual Studio 2022 على Windows؛ لا يمكن تأكيد WinForms في Linux.
- [x] إضافة زر «تنفيذ» يشغّل ملف MIPS الناتج عبر محاكي حقيقي ويعرض stdout داخل المحرر، من دون تحويل إلى لغة آلة أو تنفيذ مفسر بديل.
- [x] تحقق Linux لعميل التنفيذ: شغّل `program.asm` الرسمي في SPIM وأظهر `15`، كما عالج غياب المحاكي بـ`MipsRuntimeUnavailableException`.
- [ ] توثيق وضبط مسار محاكي MIPS على Windows واختبار نجاح التنفيذ وتعذر المحاكي أو فشل التنفيذ داخل واجهة Windows Forms.
- [ ] إضافة واجهة إدخال runtime للبرامج التي تستخدم `اقرأ` إذا طُلب تشغيلها من زر «تنفيذ» داخل المحرر؛ الإصدار الحالي يدعم البرامج غير التفاعلية فقط.
- [ ] تنفيذ طلب الدكتور لإنتاج برنامج Windows قابل للتشغيل بعد TAC، مع توثيق وفصل المسار الصحيح بين x86 Assembly الأصلي وCIL/ILAsm وعدم خلط MIPS مع أدوات Windows التنفيذية.
- [ ] إضافة هدف Windows تنفيذي مستقل: `TAC → CIL (.il) → ilasm.exe → program.exe → stdout`، مع إبقاء MIPS كهدف منفصل وعدم وصف CIL بأنه x86 Assembly.
- [ ] اختبار بناء وتشغيل `program.exe` وناتج `Console` من محرر Windows Forms على Windows قبل اعتبار طلب الدكتور محققاً.
- [x] إضافة `CilExecutableGenerator` لإنتاج `program.il` من TAC المدعوم، مع إبقاء MIPS هدفاً مستقلاً ومع تشخيص `CIL001` للقدرات غير المدعومة في EXE.
- [x] إضافة artifacts `program.il` و`windows-executable-diagnostics.txt` وحقول CIL/EXE في manifest، وعرض مراحل CIL وWindows EXE ونتيجة التنفيذ في Compiler Journey.
- [x] تحقق عملي متوافق: جُمع CIL الناتج إلى EXE عبر ILAsm محلي وشغّل بناتج `15` للعينة الرسمية و`40` لمثال `10 + 30`.
- [x] توسعة مجموعة LL(1) الرسمية باختبار CIL؛ نجحت **26/26**.
- [ ] استبدال صف أزرار المراحل المزدحم بخمس محطات رئيسية: المصدر، التحليل، التمثيل الوسيط، الأهداف، والنتيجة، مع قائمة مراحل فرعية تظهر في سياق المحطة المختارة فقط.
- [ ] إعادة تنظيم شريط الأوامر إلى أوامر ملف ثانوية وزر ترجمة أساسي وزر تنفيذ EXE مشروط بالنجاح، مع حالات تركيز وتعطيل ورسائل حالة نصية قابلة للقراءة.
- [ ] فحص الواجهة الجديدة على Windows بأحجام نافذة صغيرة ومتوسطة وكبيرة، وتأكيد عدم وجود تداخل أو قص في العربية أو MIPS/CIL قبل اعتماد التصميم.
- [x] استبدال صف مراحل 12 زراً بخمس محطات رئيسية وقائمة فرعية سياقية في `CompilerJourneyForm`، مع نقل إجراءات MIPS وEXE إلى محطة الأهداف.
- [x] تحقق صياغة C# لملفات Forms عبر Roslyn: 6 ملفات بلا أخطاء صياغة؛ بقي بناء WinForms النهائي على Windows مفتوحاً.
- [x] تحقق بعد إعادة التصميم: نجح بناء Core وCLI واختبارات LL(1) الرسمية **26/26**.

## نشر CLI على Windows

- [x] نُشر المترجم المستقل إلى `publish/Bayan.Compiler.Cli-win-x64/` بالأمر `dotnet publish -c Release -r win-x64 --self-contained false`.
- [x] يحتوي مجلد النشر على `Bayan.Compiler.Cli.exe` وملفات `.dll` و`.deps.json` و`.runtimeconfig.json` اللازمة لتشغيل CLI على Windows مع .NET 6 Desktop/Runtime.
- [ ] نشر `Bayan.Editor.WinForms.exe` بعد نجاح Rebuild المحرر وقائمة التحقق على Windows.

## حزمة ZIP

- [x] إنشاء الحزمة السابقة `/home/ubuntu/BayanCompiler_Official_Project.zip` مع استبعاد مخرجات `bin` و`obj` وartifacts التشغيل والمجلدات المؤقتة؛ استُبدلت بعد إصلاح بناء Windows.
- [x] إنشاء الحزمة المصححة `/home/ubuntu/BayanCompiler_Official_Project_WindowsBuildFix_2026-08-24.zip` وفحصها: **102 ملفاً**، و**25 عينة**، و`Bayan.sln` وملفات الإصلاح والتشخيص موجودة، وعدد مسارات `bin` أو`obj` فيها صفر. SHA-256: `065871162de8e9b7bfc8084098dd1d668908ec52726934f9f4aa352bf30cbe1c`.
- [x] إنشاء الحزمة المتوافقة مع Windows `/home/ubuntu/BayanCompiler_Windows_Compatible_2026-08-24.zip` وفحصها: **104 ملفات**، و**25 عينة `.bayan`**، وCLI منشور لـWindows، وجميع المسارات داخل ZIP هي ASCII English. SHA-256: `d8acca0d7d5b91befc67296a614ab08f063c8464cdfc9befca95414c090332b8`.
- [x] إنشاء حزمة الواجهة الجديدة `/home/ubuntu/BayanCompiler_BayanSourceStudio_2026-08-24.zip` وفحصها: **106 ملفات**، و**25 عينة `.bayan`**، و`WelcomeForm.cs` وتوثيق Bayan Source Studio موجودان، وجميع المسارات داخل ZIP هي ASCII English. SHA-256: `d4c75103e88ad78ba2502821e8b8e380ea7dc80f29d3fbb021ae8a32a2c056b9`.
- [x] إنشاء حزمة Compiler Journey `/home/ubuntu/BayanCompiler_CompilerJourney_2026-08-24.zip` وفحصها: **111 ملفاً**، و**25 عينة `.bayan`**، و`CompilerJourneyForm.cs` ووثائق التصميم والتحقق موجودة، وجميع المسارات داخل ZIP هي ASCII English. SHA-256: `e497e4110778de693a3676758564ca1e3c782edf42fe73f2061f329025b1b5ba`.
- [x] إنشاء حزمة زر التنفيذ `/home/ubuntu/BayanCompiler_MipsExecution_2026-08-24.zip` وفحصها: **114 ملفاً**، و**25 عينة `.bayan`**، و`MipsRuntimeClient.cs` ودليل إعداد المحاكي موجودان، وجميع المسارات داخل ZIP هي ASCII English. SHA-256: `c0a5de4c1256283014bdb37bb361cc37c4caf1fd48512dbba5f29715398533b9`.
- [x] إنشاء حزمة الهدف التنفيذي CIL `/home/ubuntu/BayanCompiler_CilExecutable_2026-08-24.zip` وفحصها: **120 ملفاً**، و**25 عينة `.bayan`**، ومولد CIL وباني ILAsm وعميل EXE ووثائق القرار والتحقق موجودة، وجميع المسارات داخل ZIP هي ASCII English. SHA-256: `54c0e35c645dd9672767855986ea441aae728ab78f9fdbd88d08359086aa2dd4`.

## قيد تحقق بيئي حالي

حاولت بناء المحرر في Linux مع `EnableWindowsTargeting=true`. توقف البناء عند غياب `Microsoft.NET.Sdk.WindowsDesktop.targets` من SDK المثبت محلياً، وهي حزمة سطح مكتب Windows غير متاحة في هذه البيئة. لم يظهر خطأ C# من MainForm؛ يجب تنفيذ Rebuild واختبار المحرر في Visual Studio 2022 على Windows قبل التسليم النهائي.

## إصلاح بناء Windows المبلّغ

- [x] تشخيص وإصلاح دخول ملفات Windows Forms أو assembly attributes إلى مشروع `Bayan.Compiler.Core`: استُبعدت `bin/**/*.cs` و`obj/**/*.cs` من glob المصدر، وهو السبب الجذري لـ`CS0579` و`CS0234`.
- [x] تشخيص أثر المرجع المفقود إلى `Bayan.Compiler.Core.dll` في CLI (`CS0006`): هو أثر لفشل Core، وبناء CLI نجح بعد إصلاح Core.
- [x] إزالة استيراد `BayanCompiler.CodeGeneration` القديم وغير المستخدم من `Forms/MainForm.cs`، وهو سبب `CS0234` في مشروع المحرر.
- [ ] تنفيذ `Clean Solution` ثم `Rebuild Solution` وتشغيل المحرر في Visual Studio 2022 على Windows للتحقق النهائي من ترتيب البناء وتكامل CLI.
